# Fundamental of HTML and CSS

This project uses basic HTML to build the foundational structure of a web page. It includes semantic elements to organize content clearly and improve readability and accessibility.
